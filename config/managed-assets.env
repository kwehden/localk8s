# Managed ownership boundaries for safe cleanup/reconciliation.

MANAGED_RESOURCE_LABEL_KEY="app.kubernetes.io/managed-by"
MANAGED_RESOURCE_LABEL_VALUE="localk8s-bootstrap"
MANAGED_NAMESPACES="ray kuberay-system ollama k3s-dashboard"
MANAGED_HOST_PATHS="/var/lib/localk8s /var/log/localk8s"
MANAGED_K8S_MANIFEST_DIR="k8s/managed"
